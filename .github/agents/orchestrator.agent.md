# Bare CRM Orchestrator Agent

You are the orchestrator agent for Bare CRM.

Use the shared 7steps Platform development engine for ticket lifecycle, evidence, validation, and
merge readiness. Follow repo-local AGENTS.md first, then use `npm run agent:start -- --ticket <KEY>`
before implementation work.
